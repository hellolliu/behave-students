spring:
  profiles:
    active: db, core, wx
  message:
    encoding: UTF-8

server:
  port: 8082

logging:
  level:
    root:  ERROR
    org.springframework:  ERROR
    org.mybatis:  ERROR
    org.java.behave.wx:  DEBUG
    org.java.behave:  ERROR